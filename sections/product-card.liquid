{% for product in collections.all.products %}
<product-card 
    data-image="{{ product.featured_image | img_url: '300x300' }}" 
    data-title="{{ product.title }}" 
    data-price="{{ product.price | money }}" 
    data-link="{{ product.url }}">
</product-card>
{% endfor %}
